SOURCES = combination.cpp permutation.cpp RcppExports.cpp
UTILS = utils/combination.c utils/multiset_combination.c utils/k_permutation.c utils/n_tuple.c utils/multicombination.c utils/permutation.c
OBJECTS = $(UTILS:.c=.o) $(SOURCES:.cpp=.o)

all: $(SHLIB)

$(SHLIB): $(OBJECTS)

clean:
	rm -f $(SHLIB) $(OBJECTS)
