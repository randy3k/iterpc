PKG_LIBS = -Lmbcomb -lmbcomb

SOURCES = $(wildcard *.cpp) 
UTIL = $(wildcard utils/*.c)
OBJECTS = $(UTIL:.c=.o) $(SOURCES:.cpp=.o)

all: subclean $(SHLIB)

$(SHLIB): $(OBJECTS) sublibs

sublibs: subclean
	@(cd mbcomb && CFLAGS="$(CFLAGS)" CC="$(CC)" $(MAKE) libmbcomb.a)

clean: subclean
	rm -f $(SHLIB) $(OBJECTS)

subclean:
	@(cd mbcomb && $(MAKE) clean)
